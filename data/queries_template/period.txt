使用Python生成以下程式碼，只返回代碼，不需其他解釋。

指定一個空的dataframe給變數res4。

使用locals()判斷變數period是否存在，將結果指定給變數period_cond。
以下為if-else判斷式:
如果變數period_cond成立，執行以下動作:

使用for迴圈依序抓出變數period中的值，每個值有兩個變數，變數名稱為start和end。
以下為for迴圈中的動作:
判斷變數data的欄位start_time是否大於等於變數start，將結果指定給變數cond1。
判斷變數data的欄位start_time是否小於等於變數end，將結果指定給變數cond2。
對變數data進行條件篩選，返回達成(cond1 & cond2)的所有列，將結果指定給變數df4。
使用pd.concat()合併變數res4、變數df4，並將結果指定給變數res4。

複製變數res4並指定給變數data。

for迴圈結束。
if-else判斷式結束。