使用Python生成以下程式碼，只返回代碼，不需其他解釋。

從flask_restful載入Resource
從flask載入request和jsonify
載入pandas命名為pd
載入pymysql

定義一個{id}類別，此類別繼承Resource類別的功能。
在{id}類別中定義一個get()方法，方法內容如下。

param()

conn = sql_con()

SQL敘述句1:查詢a_combination_info資料表，並回傳所有欄位。
使用pd.read_sql()查詢，參數sql等於SQL敘述句1、con等於變數conn，將結果指定給變數df1。
抓出df1的comb_name欄位在combs之中的所有行，將結果指定給變數res1。

SQL敘述句2:查詢m_table_map資料表，並回傳所有欄位。
使用pd.read_sql()查詢，參數sql等於SQL敘述句2、con等於變數conn，將結果指定給變數df2。
使用pd.merge()方法將res1、df2依照camera_name欄位合併，將結果指定給變數res2。
抓出res2的(t_name、comb_name)欄位，並轉為numpy格式，將結果指定給變數res2。
複製res2並指定給變數data。

get_data()

condition()

statistic()

使用to_dict()將data轉為字典，參數orient等於'records'，將結果指定給變數res_dict。
建立一個字典，key1為type，value1為0；key2為data，value2為res_dict，將結果指定給變數result。

關閉SQL連線。

使用jsonify將result傳回。