使使用Python生成以下程式碼，只返回代碼，不需其他解釋。

使用pymysql查詢MySQL資料庫，完成後關閉連線。
將資料儲存成csv檔及html檔，並保留欄位名稱。
SQL敘述語句使用format()方法來將參數傳入。

MySQL連線資訊: 
    host: 192.168.1.206
    port: 3306
    user: aiuser
    password: 123EWQasdCXZ
    db: hermesai

用sys.argv接收命令列參數:
    1. param1
    2. param2
    3. param3
    4. param4

    其中1、2為時間，在SQL敘述句中須以(')包住。

SQL敘述句1:
    查詢m_table_map資料表。
    回傳t_name, camera_name欄位。

    篩選條件:
        camera_name in (
            查詢a_combination_info資料表。
            回傳camera_name欄位。

            篩選條件:
                comb_name in (param3)
        )

tables = 回傳結果中的t_name欄位。
印出tables。

依照以下SQL敘述句2，依序查詢tables中的所有資料表。

SQL敘述句2:
    查詢tables中的其中一個資料表
    回傳所有欄位。
    
    篩選條件:
        start_time >= param1
        且
        end_time <= param2
        且
        (
            如果param4 = "1"就增加條件:
            false_triggered != 0
        )

合併所有的查詢結果。

將查詢結果儲存成csv檔，存檔的絕對路徑為file_path_csv。

將查詢結果轉換為HTML格式(需要顯示框線)，並儲存成html檔，存檔的絕對路徑為file_path_html。